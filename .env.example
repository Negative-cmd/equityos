# Base Builder Code (base.dev > Settings > Builder Codes).
# Defaults to bc_gc827vuh in lib/wagmi-config.ts if unset — set this in
# Vercel's Environment Variables if you ever rotate the code.
NEXT_PUBLIC_BASE_BUILDER_CODE=bc_gc827vuh
